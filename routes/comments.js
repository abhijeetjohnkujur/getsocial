const express = require('express');
const router = express.Router();
const passport = require('passport');

const commetController = require('../controllers/commentsController');


module.exports = router;
